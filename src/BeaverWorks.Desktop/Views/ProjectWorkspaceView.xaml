<UserControl x:Class="BeaverWorks.Desktop.Views.ProjectWorkspaceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:BeaverWorks.Desktop.ViewModels"
             xmlns:local="clr-namespace:BeaverWorks.Desktop.Views"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:ProjectWorkspaceViewModel}"
             d:DesignWidth="960" d:DesignHeight="600">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="1*" MinWidth="240" />
        </Grid.ColumnDefinitions>

        <local:PlanCanvasView Grid.Column="0" DataContext="{Binding Canvas}" />
        <local:TaskListView Grid.Column="1" DataContext="{Binding TaskList}" />
    </Grid>
</UserControl>
