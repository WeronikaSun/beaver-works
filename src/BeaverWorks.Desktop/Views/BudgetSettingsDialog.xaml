<Window x:Class="BeaverWorks.Desktop.Views.BudgetSettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:BeaverWorks.Desktop.Views"
        xmlns:vm="clr-namespace:BeaverWorks.Desktop.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:BudgetSettingsViewModel}"
        Title="Budget settings"
        Width="360"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Window.Resources>
        <local:NullOrEmptyStringToVisibilityConverter x:Key="NullOrEmptyStringToVisibilityConverter" />
    </Window.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Weekly time budget (hours)" Margin="0,0,0,4" />
        <TextBox Grid.Row="1"
                 Text="{Binding WeeklyTimeBudgetHours, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,12" />

        <TextBlock Grid.Row="2" Text="Monthly money budget" Margin="0,0,0,4" />
        <TextBox Grid.Row="3"
                 Text="{Binding MonthlyMoneyBudget, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,12" />

        <TextBlock Grid.Row="4"
                   Text="{Binding ErrorMessage}"
                   Foreground="Red"
                   TextWrapping="Wrap"
                   Margin="0,0,0,8"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource NullOrEmptyStringToVisibilityConverter}}" />

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Command="{Binding CancelCommand}" Padding="12,4" Margin="0,0,8,0" />
            <Button Content="Save" Command="{Binding SaveCommand}" Padding="12,4" IsDefault="True" />
        </StackPanel>
    </Grid>
</Window>
