<UserControl x:Class="BeaverWorks.Desktop.Views.TaskListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:BeaverWorks.Desktop.ViewModels"
             xmlns:local="clr-namespace:BeaverWorks.Desktop.Views"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:TaskListViewModel}"
             d:DesignWidth="280" d:DesignHeight="480">
    <UserControl.Resources>
        <local:TaskStatusToBrushConverter x:Key="TaskStatusToBrushConverter" />
        <local:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
    </UserControl.Resources>
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="2*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="3*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Tasks" FontWeight="Bold" Margin="0,0,0,4" />

        <ListBox Grid.Row="1"
                  x:Name="TasksListBox"
                  ItemsSource="{Binding Tasks}"
                  SelectedItem="{Binding SelectedTask, Mode=TwoWay}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Ellipse Width="10" Height="10" Margin="0,0,6,0"
                                 Fill="{Binding Status, Converter={StaticResource TaskStatusToBrushConverter}}" />
                        <TextBlock Text="{Binding Title}" VerticalAlignment="Center" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <TextBlock Grid.Row="2" Text="Details" FontWeight="Bold" Margin="0,12,0,4" />

        <StackPanel Grid.Row="3" Orientation="Vertical"
                    Visibility="{Binding SelectedTask, Converter={StaticResource NullToVisibilityConverter}}">
            <TextBlock Text="{Binding SelectedTask.Title}" FontWeight="Bold" TextWrapping="Wrap" Margin="0,0,0,4" />
            <TextBlock Text="{Binding SelectedTask.Description}" TextWrapping="Wrap" Margin="0,0,0,8" />

            <TextBlock Text="Status" FontWeight="SemiBold" />
            <ComboBox x:Name="StatusComboBox"
                      ItemsSource="{Binding StatusOptions}"
                      SelectedItem="{Binding SelectedTask.Status, Mode=OneWay}"
                      SelectionChanged="StatusComboBox_SelectionChanged"
                      Margin="0,0,0,8" />

            <TextBlock Margin="0,0,0,4">
                <Run Text="Priority: " FontWeight="SemiBold" />
                <Run Text="{Binding SelectedTask.Priority, Mode=OneWay}" />
            </TextBlock>
            <TextBlock Margin="0,0,0,4">
                <Run Text="Estimated cost: " FontWeight="SemiBold" />
                <Run Text="{Binding SelectedTaskEstimatedCostDisplay, Mode=OneWay}" />
            </TextBlock>
            <TextBlock Margin="0,0,0,4">
                <Run Text="Estimated time: " FontWeight="SemiBold" />
                <Run Text="{Binding SelectedTaskEstimatedTimeDisplay, Mode=OneWay}" />
            </TextBlock>
            <TextBlock Margin="0,0,0,4">
                <Run Text="Room: " FontWeight="SemiBold" />
                <Run Text="{Binding SelectedTaskRoomDisplay, Mode=OneWay}" />
            </TextBlock>
            <TextBlock TextWrapping="Wrap" Margin="0,0,0,8">
                <Run Text="Depends on: " FontWeight="SemiBold" />
                <Run Text="{Binding SelectedTaskDependencySummary, Mode=OneWay}" />
            </TextBlock>

            <StackPanel Orientation="Horizontal">
                <Button Content="Edit" Command="{Binding EditCommand}" Padding="12,4" Margin="0,0,8,0" />
                <Button Content="Delete" Command="{Binding DeleteCommand}" Padding="12,4" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
